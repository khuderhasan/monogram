 class Tabs {
   static const feed = Tabs(0);
   static const notification = Tabs(1);
   static const community = Tabs(2);
   static const settings = Tabs(3);

   final int value;

   const Tabs(this.value);
}